@*@using WebGrease.Css.Extensions
@model IEnumerable<EmployeesManagementSystem.Models.TeamEmployees>

@{
    ViewBag.Title = "Details of all employees in a team";
}

<h2>Details of all employees in team @ViewBag.TeamName </h2>

<p>
    @Html.ActionLink("Add Employee", "AddEmployee")
</p>
<table class="table">
    <tr>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Job Position
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobPosition)
        </td>
        <td>
            @Html.ActionLink("Delete", "DeleteEmployee", new { id = item.TeamId })
        </td>
    </tr>
}

</table>*@
